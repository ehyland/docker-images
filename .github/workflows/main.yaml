on: [push]

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - run: docker login -u ehyland -p ${{ secrets.DOCKER_HUB_TOKEN }}

      - run: images/verdaccio-test/scripts/build.sh push
